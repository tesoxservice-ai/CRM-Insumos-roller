// next.config.ts
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Sin rewrites especiales por ahora; el proyecto comparte la DB de Supabase
  // con el configurador de cortinas pero es un proyecto Next.js independiente.
}

export default nextConfig
